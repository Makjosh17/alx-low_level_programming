#include <stdio.h>
/**
 * main - Entry point
 * Return: 0
 */
int main (void)
{
	char x = 'a';

	while (x <= 'z')
	{
		if (x != 'e' && x != 'q')
		{
			putchar(x);
		}
		c++;
	}
	putchar('\n');
	return (0);
}
